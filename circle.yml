machine:
  node:
    version: 4.1.0
dependencies:
  override:
    - npm install
test:
  override:
    - textlint --cache -f checkstyle _i18n/ja/ | reviewdog -f=checkstyle -name="textlint" -ci="circle-ci"
general:
  branches:
    ignore:
      - master