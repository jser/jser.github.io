version: 1
rules:
  - expected: 対処法
    pattern:  対処方
    specs:
      - from: 対処方
        to:   対処法
  - expected: ように$1
    pattern: /用に(な?)/
    specs:
      - from: 用になっている
        to: ようになっている
  - expected: 行える
    pattern:  行得る